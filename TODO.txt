- (Non)Unique index support.

- Where clause on joins.

- Multiple joins on same table.

- Meta tables (systables, syscolumns).

- Aggregates on result sets, e.g. Group By. (CGroupSet - Array of RS).

- Computations on result sets, e.g. distinct, sum, min, max, avg. (CValueSet).

- Special asserts.

- Change headers from .hpp to .h

- Query processor.

- MERGE CHANGES FROM WORK.
